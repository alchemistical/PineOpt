# Production Environment Configuration
# Copy to .env and modify as needed

# Application Settings
FLASK_ENV=production
NODE_ENV=production
PORT=5001
FRONTEND_PORT=80

# Database Settings
DATABASE_PATH=/app/database/pineopt.db
STRATEGIES_DB_PATH=/app/api/strategies.db

# API Settings
API_BASE_URL=http://backend:5001
CORS_ORIGINS=http://localhost,http://localhost:80

# Market Data Settings
BINANCE_API_TIMEOUT=30
TARDIS_API_TIMEOUT=30
CACHE_EXPIRY_SECONDS=300

# Backtesting Settings
MAX_CONCURRENT_BACKTESTS=5
BACKTEST_TIMEOUT_MINUTES=10
MAX_BACKTEST_HISTORY_DAYS=365

# Security Settings
SECRET_KEY=change-this-in-production-to-a-random-key
ALLOWED_HOSTS=localhost,127.0.0.1
MAX_UPLOAD_SIZE_MB=10

# Logging Settings
LOG_LEVEL=INFO
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Performance Settings
WORKERS=4
THREADS=2
MAX_REQUESTS=1000
MAX_REQUESTS_JITTER=100

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=100

# Feature Flags
ENABLE_MONITORING=true
ENABLE_CACHING=true
ENABLE_RATE_LIMITING=true